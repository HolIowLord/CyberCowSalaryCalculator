# 开发日志

## 2026-06-11 初步可用版

### 已完成

- 主界面保留核心信息：工作时长、今日收入、上班、摸鱼、下班。
- 薪资设置支持工作时间、底薪、加班费/h、绩效、奖金。
- 今日收入支持正常工时和加班工时分段计算。
- 隐私模式支持薪资非明文显示：
  - 今日收入显示为 `****`
  - 底薪、加班费、绩效、奖金输入框显示为固定 `******`
  - 薪资摘要隐藏具体金额
  - 自动折算时薪保持明文，便于核算
- 新增“重置今日数据”，清空今日计时但保留薪资配置。
- 新增摸鱼按钮，点击后停止计时并调用 Windows 系统锁屏。
- 窗口改为可拖动、可自由缩放的 Electron 无边框窗口。
- 关闭按钮改为确认弹窗：
  - `关闭`：退出并清除当前进程
  - `最小化`：隐藏到后台并显示系统托盘图标
- 新增系统托盘图标，支持恢复窗口和退出。
- 接入程序图标资源：
  - `assets/app-icon.ico`
  - `assets/app-icon.png`
- 新增发布版首次启动初始化：
  - 首次运行时清理旧 `localStorage` 测试数据
  - 写入初始化标记，避免后续重复清空用户数据

### 重要设计调整

- 取消“设置中选择窗口宽度”的方案。
- 取消自动测量内容并强行调整窗口尺寸的方案。
- 改为由用户拖动窗口边缘缩放，UI 在当前窗口内自适应。

### 待观察

- 托盘图标在不同 Windows 缩放比例下的显示效果。
- 重新打包后 exe 嵌入图标是否符合预期。
- 极小窗口下是否需要进一步切换为单列布局。
