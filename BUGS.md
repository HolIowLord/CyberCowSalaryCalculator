# 已知问题

## 工作时间输入框宽度不一致

状态：已记录，暂缓处理。

现象：薪资设置页的“上班 / 下班”两个原生 `time` 输入框在部分环境中宽度可能不完全一致。

原因：浏览器原生 `input[type="time"]` 带有内部选择图标和最小宽度策略，不同 Chromium/Electron 环境显示略有差异。

后续方向：用自定义时间选择组件替代原生 `time` 输入，或改为普通文本输入加格式校验。

## 窄窗口下文字可能被压缩

状态：已缓解，继续观察。

现象：窗口被用户缩得很窄时，工作时长或金额仍可能出现截断。

当前处理：使用响应式字号和可缩放窗口，主流程已可用。

后续方向：如果继续追求极窄窗口体验，可在窄宽度下改成单列布局。

## 设置页在极小窗口下需要滚动

状态：符合当前设计，继续观察。

现象：窗口高度较小时，设置页内容需要滚动查看。

说明：当前采用用户自由缩放窗口策略，不再自动强行拉高窗口。设置页使用内部滚动保持功能可用。

## 托盘图标和 exe 图标需要实机确认

状态：已接入图标资源，待打包/实机复测。

现象：已将 `manual-release\赛博牛马薪资计算器\tubiao` 下的图片转换为 `assets/app-icon.ico` 和 `assets/app-icon.png`，并接入窗口、托盘和打包配置。

说明：手动运行目录会使用运行时图标；如果要改变 exe 文件本身的嵌入图标，需要重新执行 `npm.cmd run dist` 打包。

## 历史窗口尺寸问题

状态：已改变策略，旧问题不再按原方案处理。

说明：此前尝试自动测量内容并调整窗口尺寸，容易造成透明留白、打开设置后窗口变大等问题。当前版本已取消自动贴合，改为 Electron 原生可缩放窗口，由用户拖动边缘控制大小。
